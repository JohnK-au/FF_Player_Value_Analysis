# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
.venv-tabfm/
venv/
env/
.python-version

# Jupyter
.ipynb_checkpoints/

# Environment & secrets (never commit credentials)
.env
*.env
credentials.json
service_account.json
*token*.json
secrets/

# Raw data (keep large/private data out of git; commit small processed samples deliberately)
data/raw/
*.csv
*.xlsx
*.parquet

# Allowlist: subjective-override files are user-edited and must persist across sessions
!data/research/intangibles_overrides.csv
!data/research/user_comparisons.csv
!data/research/player_comments.csv
!data/research/user_valuations.csv
!data/research/comparison_analysis.csv

# Allowlist: static research snapshot (public NFL stats + our scoring totals; no
# salaries/IDs/secrets). Committed so the PyTorch work is reproducible on any
# device without ESPN creds — see docs/research/wr_weekly_torch.md §6.
!data/processed/research/wr_weekly_features.csv

# Figures (contain league data — names, salaries)
figures/
*.png

# Personal working notes (task/idea backlog — not part of the public repo)
TODOs.md

# OS / editor
.DS_Store
.idea/
.vscode/
streamlit_log.txt
streamlit_err.txt

# TabFM scratch "current step" panel (kept open in a side tab, not docs)
docs/research/tabfm/_now.md
